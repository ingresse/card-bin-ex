version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: mix local.hex --force
      - run: mix local.rebar --force
      - run: mix deps.get
      - run: MIX_ENV=test mix coveralls.html
      - store_artifacts:
          path: ./cover
